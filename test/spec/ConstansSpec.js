describe('* Constants', function() {
  it('should have genders varible',function(){
    expect(genders).not.toEqual(null);
    expect(genders).toBeDefined;
  });
  it('should have ahcountries varible',function(){
    expect(ahcountries).not.toEqual(null);
    expect(ahcountries).toBeDefined;
  });
  it('should have religions varible',function(){
    expect(religions).not.toEqual(null);
    expect(religions).toBeDefined;
  });
  it('should have maritalstatuses varible',function(){
    expect(maritalstatuses).not.toEqual(null);
    expect(maritalstatuses).toBeDefined;
  });
  it('should have documentType varible',function(){
    expect(documentType).not.toEqual(null);
    expect(documentType).toBeDefined;
  });
  it('should have ah_employeetypes varible',function(){
    expect(ah_employeetypes).not.toEqual(null);
    expect(ah_employeetypes).toBeDefined;
  });
  it('should have ah_departments varible',function(){
    expect(ah_departments).not.toEqual(null);
    expect(ah_departments).toBeDefined;
  });
  it('should have world_countries varible',function(){
    expect(world_countries).not.toEqual(null);
    expect(world_countries).toBeDefined;
  });
});
